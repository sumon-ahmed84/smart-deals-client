import React from 'react';
import { useLoaderData } from 'react-router';

const ProductsDetales = () => {
    const product = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default ProductsDetales;